# This file contains the URL patterns specific to the 'mainapp' app. It defines how URLs associated with this app should be handled.

from django.urls import path
from . import views

urlpatterns = [
    # define app specific URLs here
]
